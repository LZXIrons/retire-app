<!--pages/subPages//hotSpotPolicy/hotSpotPolicy.wxml-->

<van-tabs line-width="0" title-active-color="#e25453" bind:change="changeNav" bind:click="clickNav" id="tabs" sticky>
  <view slot="nav-right" class="special-nav">
    <van-dropdown-menu>
      <block wx:for="{{specialNavTit}}" wx:key="id">
        <van-dropdown-item id="{{item.type == 'areaList'? 'item_area' : 'item_' + index}}" title="{{item.title}}"
          bind:open="openHandle" bind:close="closeHandle">
          <block wx:if="{{item.type == 'areaList'}}">
            <van-area area-list="{{ item.option }}" id="area" columns-num="{{ 3 }}" bind:confirm="selectAreaHandle"
              bind:cancel="cancleAreaHandle" cancel-button-text="重置" />
          </block>
        </van-dropdown-item>
      </block>
    </van-dropdown-menu>
  </view>
  <block wx:for="{{navTit}}" wx:key="id">
    <van-tab title="{{item.title}}" custom-class="wrap-mp20-box bg-white {{windowFixed?'fixed-position' : ''}}">
      <van-empty wx:if="{{isEmpty}}" image="/static/images/empty.png" description="没有数据了" />
      <view wx:else>
        <!-- 骨架屏 -->
        <block wx:for="{{6}}" wx:key="index">
          <view class="list-180h-item border-2-bottom pt46 mb20" hidden="{{isRender}}">
            <van-skeleton row="2" />
          </view>
        </block>
        <block wx:for="{{articleList}}" wx:for-item="yitem" wx:for-index="index" wx:key="idx">
          <view class="list-180h-item border-2-bottom pt46" data-yitem="{{yitem}}" catch:tap="toDetail">
            <view class="fs-26 line-2-hide"> {{yitem.title}}</view>
            <view class="mt20 fs-24 text-light-gray d-flex jc-between">
              <view>来源：{{yitem.originate}}</view>
              <view>时间：{{yitem.articleTime}}</view>
            </view>
          </view>
        </block>
      </view>
    </van-tab>
  </block>
</van-tabs>
<van-divider contentPosition="center" wx:if="{{isComplete}}">没有数据了~~</van-divider>


<!-- <van-popup show="{{ showPopup }}" position="top" bind:close="closePopup">
  <van-area area-list="{{ areaList }}" id="area" columns-num="{{ 3 }}" title="区域选择" cancel-button-text="重置"
    bind:confirm="selectAreaHandle" bind:cancel="cancleAreaHandle" />

</van-popup> -->